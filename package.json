{
  "name": "n8n-github-blog-publisher",
  "version": "1.0.0",
  "private": true,
  "description": "Website that authenticates with GitHub, triggers an n8n blog generator, and publishes returned files.",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1"
  }
}
